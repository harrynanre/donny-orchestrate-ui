import Terminal from "@/pages/user/Terminal"

export const metadata = {
  title: "Terminal - Donny Hub",
  description: "Direct AI provider connections and command execution",
}

export default function TerminalPage() {
  return <Terminal />
}